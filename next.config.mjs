/** @type {import('next').NextConfig} */
const nextConfig = {
  // pdf-parse is server-only; no special config needed with Turbopack
  // It will be handled naturally as a server-side module in API routes
};

export default nextConfig;
